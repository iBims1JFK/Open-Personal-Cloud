# Ansible Inventory
# Update with actual IP addresses or hostnames

all:
  children:
    primary:
      hosts:
        n150-node:
          ansible_host: 192.168.1.100  # REPLACE ME
          ansible_user: user
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519

    edge:
      hosts:
        vps-node:
          ansible_host: 203.0.113.1  # REPLACE ME (Hetzner IP)
          ansible_user: root

    backup:
      hosts:
        rpi-node:
          ansible_host: 192.168.1.101  # REPLACE ME
          ansible_user: pi
